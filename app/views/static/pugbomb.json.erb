{
  "attachments" :
    <%= raw(@pugs.map{|pug| Hash["image_url", pug, "fallback", "pug"]}.to_json) %>,
  "text": "<%== @response %>",
  "response_type": "<%= @private ? 'ephemeral' : 'in_channel' %>"
}
